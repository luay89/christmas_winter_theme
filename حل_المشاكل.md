# 🔧 حل المشاكل الشائعة

## مشكلة البناء على Linux

### الخطأ:
```
CMake Error: A required package was not found
```

### الحل:

**الطريقة 1: تثبيت المكتبات المطلوبة**
```bash
sudo apt-get update
sudo apt-get install -y libasound2-dev
```

ثم حاول مرة أخرى:
```bash
flutter run -d linux
```

**الطريقة 2: استخدام Chrome/Web**
```bash
flutter run -d chrome
```

**الطريقة 3: استخدام Android (الموصى به)**
```bash
flutter run -d android
```

للحصول على قائمة بالأجهزة المتاحة:
```bash
flutter devices
```

## لماذا هذه المشكلة؟

حزمة `audioplayers` تحتاج إلى مكتبات صوتية خاصة بالمنصة:
- **Linux**: يحتاج `libasound2-dev`
- **Android/iOS**: يعمل بشكل مباشر
- **Web**: يعمل عبر المتصفح

## الحل الأفضل

بما أن التطبيق مصمم بشكل أساسي لـ **Android و iOS** (كما طلبت)، يُنصح بشدة باستخدام:
1. **Android Emulator** أو جهاز Android حقيقي
2. **iOS Simulator** (على Mac) أو جهاز iOS حقيقي

## إعداد Android Emulator

إذا لم يكن لديك emulator:

```bash
flutter emulators
flutter emulators --launch <emulator_id>
```

أو استخدم Android Studio:
1. افتح Android Studio
2. Tools > Device Manager
3. أنشئ جهاز جديد (Create Device)
4. اختر صورة نظام (System Image)
5. ابدأ المحاكي
6. ثم شغل: `flutter run -d android`

## ملاحظات

- التطبيق سيعمل بشكل أفضل على Android/iOS
- تشغيله على Linux يحتاج مكتبات إضافية
- تشغيله على Web قد يكون محدوداً في بعض الميزات الصوتية

---

**للمساعدة الإضافية، راجع:** `README.md` أو `دليل_الاستخدام.md`



